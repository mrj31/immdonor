LOAD DATA LOCAL INFILE "../MySQL/workspace.txt"
    into table workspace
    fields terminated by '~@~'
    lines terminated by '~@@~\n'
    (
      workspace_id,
      category,
      name,
      type 
    );
